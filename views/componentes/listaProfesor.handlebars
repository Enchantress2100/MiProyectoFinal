<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Acceso para Profesores</title>
<link href="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-1BmE4kWBq78iYhFldvKuhfTAU6auU8tT94WrHftjDbrCEXSU1oBoqyl2QvZ6jIW3" crossorigin="anonymous">
<link rel="preconnect" href="https://fonts.googleapis.com">
<link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
<link href="https://fonts.googleapis.com/css2?family=Fira+Sans:wght@300&family=Redressed&display=swap" rel="stylesheet">
<link rel="stylesheet" href="style.css" />
<link rel="icon" type="image/x-icon" href="favicon-32x32.png">
</head>
<body>

    <div class="py-4 container table-responsive">
      <table class="table table-dark table-hover">
        <thead>
          {{#if curso}}
          <tr>
            <th scope="col">Número</th>
            <th scope="col">Foto</th>
            <th scope="col">Nombre</th>
            <th scope="col">Apellido</th>
            <th scope="col">Rut</th>
            <th scope="col">Asistencia</th>
          </tr>
          {{/if}}
        </thead>
        <tbody>
          {{#each curso}}
          <tr>
            <th scope="row">{{this.row_number}}</th>
            <td><div><img src="{{this.foto}}" width="60px" height="60px"></div></td>
            <td>{{this.nombre}}</td>
            <td>{{this.apellidos}}</td>
            <td>{{this.rut}}</td>
            <td>
              <a href="/asistencia?rut={{this.rut}}&estado=Presente"><button type="button" class="btn btn-primary">Presente</button></a>
              <a href="/asistencia?rut={{this.rut}}&estado=Ausente"><button type="button" class="btn btn-secondary">Ausente</button></a>
              <a href="/asistencia?rut={{this.rut}}&estado=Se Retira"><button type="button" class="btn btn-warning">Se Retira</button></a>
              <a href="/asistencia?rut={{this.rut}}&estado=Llega Tarde"><button type="button" class="btn btn-info">Llega Tarde</button></a>
              <a href="/asistencia?rut={{this.rut}}&estado=Sancionado"><button type="button" class="btn  btn-danger">Sancionado</button></a>
            </td>
           {{/each}}
          </tr>
        </tbody> 
      </table>
    </div>


<script src="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/js/bootstrap.bundle.min.js" integrity="sha384-ka7Sk0Gln4gmtz2MlQnikT1wXgYsOg+OMhuP+IlRH9sENBO0LRn5q+8nbTov4+1p" crossorigin="anonymous"></script>
</body>
</html>